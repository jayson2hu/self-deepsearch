# 作品与公开人物资料展示站：产品规划

版本：v1.0  
日期：2026-08-30  
状态：本轮产品决策已确认，作为 MVP 开发基线

## 1. 产品结论

产品可做，首期目标不是建设资源站，也不是做人脸识别，而是建设一个面向 18 岁以上用户的公开资料索引站：

> 以作品浏览和推荐为核心，以番号搜索和人物资料为辅助，通过稳定、可索引的详情页获取搜索流量。

网站只展示公开资料和经过审核的图片，不提供、存储、转发或跳转到视频、音频、磁力、种子、网盘、下载资源。测试期不接入广告联盟，只保留明确标注的广告位占位和后续接入能力。

产品的主要难点不是页面开发，而是：

- 番号、人物别名和多来源数据的标准化与去重；
- 图片来源、展示状态和下架能力；
- SEO 页面质量与站点性能；
- 内容审核、纠错、账号和审计流程；
- 两台小规格服务器与免费 S3 额度下的稳定运行。

## 2. 产品定位与边界

### 2.1 目标用户

- 18 岁以上，通过番号寻找作品公开资料的用户；
- 希望浏览最新发行、近期热门或人物相关作品的用户；
- 希望查询人物公开艺名、别名和可选公开参数的用户；
- 负责资料录入、审核、纠错、图片和权利处理的运营人员。

### 2.2 核心价值

1. 输入不同格式的番号，快速到达唯一作品详情页。
2. 浏览按实际发行日期排序的新作品，而不是把高点击误称为“最新”。
3. 从作品发现人物，再从人物浏览相关作品和推荐。
4. 在资料缺失或图片缺失时仍提供结构稳定的页面和默认图。
5. 为每项资料保留来源类型、核验时间和内部审计依据。

### 2.3 明确不做

- 视频或音频播放、下载、磁力、种子、网盘和资源链接；
- 资源网站跳转、自动跳转、伪装下载按钮；
- 头像识别、人脸搜索、人脸向量库或公开识别 API；
- 评论、私信、公开用户主页、公开收藏夹或用户社交关系；
- 用户直接上传文件或让用户提交内容自动发布；
- 测试期广告联盟脚本、广告结算、倒计时关闭和强制插屏；
- Redis、OpenSearch、ClickHouse、Kubernetes 和微服务拆分。

## 3. 信息架构

### 3.1 首页

首页默认按以下顺序展示：

1. 搜索与主导航；
2. 最新发行作品；
3. 登录用户关注人物的新作品；
4. 热门人物及代表作品；
5. 近期热门；
6. 浏览最多；
7. 编辑推荐；
8. 页脚资料说明、18+ 提示和权利联系邮箱。

默认发现流以作品为主、人物为辅，首期默认采用 `作品 : 人物 = 4 : 1` 的槽位比例，并由后台配置而不是写死在前端。人物卡片用于增加继续浏览入口，不能挤占最新作品的主体位置。Release A 先保证同一实体卡不重复；同一人物或厂牌跨多部作品的窗口重复抑制，需要真实目录验证后再调优，不作为测试站放行门槛。

### 3.2 排序口径

| 页面/标签 | 排序依据 |
| --- | --- |
| 最新发行 | 作品实际发行日期倒序 |
| 最近收录 | 本站首次收录时间倒序 |
| 近期热门 | 最近 7 天详情页请求次数 |
| 浏览最多 | 最近 30 天详情页请求次数 |
| 历史最多 | 累计详情页请求次数 |
| 编辑推荐 | 管理员配置的有效期、顺序和理由 |

实际发行日期不可靠或缺失的作品不得进入“最新发行”，只能进入“最近收录”或“待核验”。

### 3.3 推荐规则

首期不使用机器学习，相关作品按以下权重生成候选：

```text
同一人物作品       40%
同厂牌或系列       25%
相同受控标签       20%
发行日期接近       10%
人工推荐            5%
```

推荐结果必须过滤未发布、隐藏、权利下架和重复作品。权重是候选配额，不是对用户做个人画像。

## 4. 搜索与 SEO

### 4.1 搜索字段

首期只搜索以下已确认字段：

- 作品番号；
- 作品标题；
- 人物公开艺名；
- 人物别名；
- 厂牌或制作方。

输入先进行 Unicode、全角半角、大小写、空格和番号分隔符归一。例如 `MK-1042`、`mk 1042` 和 `MK1042` 应定位到同一作品候选。

排序优先级：番号精确匹配、番号前缀、人物艺名/别名、作品标题、厂牌、模糊匹配。疑似重复不能由搜索流程自动合并。

### 4.2 SEO 页面

作品详情页是主要 SEO 落地页，番号是第一关键词，但页面不能只堆番号：

```text
URL: /works/mk-1042-a1b2
Title: MK-1042｜作品标题｜人物艺名｜厂牌
H1: MK-1042 作品标题
Description: 番号、发行日期、人物和厂牌等公开资料摘要
```

`a1b2` 是内部稳定 ID 的短表示，用于处理番号复用。编号、标题或艺名改变时，旧地址 `301` 到当前 canonical URL。

允许索引：

- 已发布作品、人物、厂牌和管理员维护的固定专题页。

默认 `noindex`：

- 任意搜索结果、筛选参数、登录、收藏、历史、反馈、后台、草稿、待核验和隐藏页面。

站点地图按作品、人物和厂牌拆分；下架内容必须从站点地图和缓存中移除。

## 5. 展示资料与图片

### 5.1 作品字段

必填：

```text
work_id
canonical_code
title
publication_status
```

可选：

```text
release_date
title_original
code_aliases
performers
studio
summary
tags
source_url
```

实际发行日期未知时可以留空，但该作品不得进入“最新发行”，只能进入“最近收录”或待核验队列。

`work_id` 使用 UUID。番号不是主键，也不设置为全局唯一；使用番号、厂牌、发行日期和来源映射共同判重。重发版或不同版本通过 `edition_of` 关联。

### 5.2 人物字段

必填：

```text
performer_id
display_name
adult_status
publication_status
```

可选：

```text
name_original
romanized_name
aliases
birth_year
height_cm
measurements
debut_year
agency
official_links
avatar_asset_id
```

人物使用永久 UUID。艺名、别名和来源站 ID 都不能作为主键。疑似同一人物必须人工确认合并；合并后旧 ID 保留重定向。

### 5.3 图片规则

- 每部作品详情页首期最多展示 1 张主图和 3 张精选图，共 4 张；列表页只展示主图；
- 作品主图固定为 `cover / primary / position 0`，精选图固定为 `gallery / non-primary / position 1-3`，同一位置只能登记一张；
- 人物头像固定为 `avatar / primary / position 0`；作品没有有效主图时，不把精选图提升为封面，直接使用作品默认图；
- 详情页使用比列表更清晰的派生图；
- 无图时必须显示作品默认图或人物默认图；
- 人物和作品使用不同默认图；
- 原始大图处理成功后删除，只保留标准化私有母版和公开派生图；
- 私有母版最长边建议 1600-2000px；
- 公开派生尺寸为 320、640、960；
- 单图最大 10MB，限制总像素，移除 EXIF，拒绝 SVG 和异常动画；
- 页面使用固定宽高、`srcset` 和懒加载，避免布局跳动。

图片来源 URL 可以为空，但内部必须记录来源类型、核验时间、操作人、置信度和权利状态。找不到可发布图片不能阻断资料发布，使用默认图兜底。

## 6. 账号与用户能力

### 6.1 匿名用户

无需注册即可浏览、搜索和查看公开详情。匿名用户不保存逐条行为、用户 ID、IP、设备指纹或访问路径。

### 6.2 注册用户

注册后提供：

- 收藏作品；
- 关注人物；
- 跨设备同步浏览历史；
- 隐藏不感兴趣作品；
- 查看关注人物更新；
- 提交纠错和反馈。

注册、忘记密码和关闭账号都必须完成邮箱验证码验证。测试期不启用 MFA。

### 6.3 前台账号入口与状态

- 游客在桌面端和移动端页头都能直接看到“登录”和“注册”，不能只显示无说明的用户图标；
- 登录、注册和忘记密码使用统一账号窗口，注册和忘记密码必须先完成邮箱验证码；
- 登录成功后页头切换为账号入口，菜单至少包含收藏作品、关注人物、浏览历史、账号设置和退出登录；
- 收藏、关注、浏览历史和资料纠错属于受保护操作，游客触发时进入登录流程，登录后才能继续；
- 登录页面和账号私有页面默认 `noindex`，邮箱不得出现在 URL、埋点或前端日志中；
- 公共页面 HTML 不读取 session，避免共享缓存污染；页头 `AccountActions` 在浏览器端通过同源 `no-store` `/api/v1/me` 加载登录状态，再切换游客入口或账号菜单。受保护页面由服务端检查 session。

### 6.4 验证码规则

```text
长度：6 位随机数字
有效期：10 分钟
重发间隔：60 秒
最大尝试：5 次
同一邮箱每小时发送：最多 3 次
同一 IP 每小时注册：最多 5 次
同一 IP 每日创建账号：最多 10 个
```

验证码只保存哈希。重新发送后旧验证码失效；使用成功后写入 `consumed_at`。注册入口使用 Cloudflare Turnstile，并统一返回提示，避免泄露邮箱是否已注册。

### 6.5 关闭账号和历史

前端操作名称为“关闭账号”，不能暗示物理删除。关闭前必须提示：账号无法继续登录，收藏、关注和历史不再展示，但审计记录及隐私说明约定的信息仍会保留。

关闭流程需要登录态和邮箱验证码；成功后：

- `account_status = closed`；
- 撤销全部会话；
- 原账号无法登录；
- 原邮箱可以注册为新的 `user_id`；
- 原账号 ID、精确时间和长期归档记录继续保留。

用户清除浏览历史时只设置 `is_deleted = true`，前端不再返回。底层记录保留，后续可压缩归档，仍保留用户 ID 和精确到秒的时间。产品隐私说明必须如实告知这一点。

### 6.6 角色

| 角色 | 能力 |
| --- | --- |
| `owner` | 邀请/停用管理员、调整角色、系统配置、下架和高风险操作 |
| `admin` | 审核、发布、隐藏、管理普通用户，不能创建 owner |
| `editor` | 编辑草稿和提交审核，不能发布 |
| `user` | 普通用户功能和反馈 |

账号邀请：`owner` 可邀请 `admin`、`editor` 或 `user`；`admin` 只能邀请 `user`；`editor` 不能发起邀请。邀请验证码只发送到目标邮箱，48 小时有效且只能接受一次；后台仅显示状态和时间，不显示验证码明文。

测试期后台使用普通密码登录；强密码、失败限流、高风险操作重新验证密码、短会话和审计日志必须启用。固定 IP/VPN 作为正式开放后台前的加固项。

## 7. 纠错与反馈

- 必须注册并登录；
- 允许文字、关联作品/人物和可选网页 URL；
- 不允许图片文件、视频、音频、压缩包、磁力、网盘或下载链接；
- 服务端不自动抓取用户提交的 URL，避免 SSRF；
- 文本过滤 HTML 和脚本；
- 账号与 IP 限频；
- 全部进入人工审核，不直接修改正式资料。

## 8. 广告

测试期不接入广告联盟，不加载第三方广告脚本，不做广告结算和事件统计。只保留以下可配置位置及“广告”标识：

- 顶部横幅；
- 列表内原生位；
- 桌面左右侧栏；
- 页面底部。

首期不做首屏插屏、强制跳转、伪装按钮和关闭倒计时。后续联盟接入必须通过统一 `AdSlot` 适配层，广告故障不能阻塞正文。

## 9. 来源、说明与权利处理

每个资料字段和逻辑图片资产内部记录：

```text
source_type
source_url（可选）
source_title（可选）
checked_at
operator_id
confidence
rights_status
```

公开可见不等于允许复制或再次传播。来源标注和免责声明也不会自动取得图片版权或免除责任。正式上线前需要针对目标地区、主机、Cloudflare、邮件和未来广告平台政策进行专业核对。

页脚至少显示：

- 18+ 提示；
- 本站仅提供资料索引；
- 不提供视频、音频、下载、磁力、网盘或资源跳转；
- 资料和图片来自经审核的公开来源，可能存在误差或延迟；
- 纠错和权利联系邮箱；
- 未来广告与本站资料不存在隶属或背书关系。

权利下架优先于普通修改：立即隐藏页面、移除搜索和站点地图、清理公开图片与 Cloudflare 缓存，并保留不含图片内容的审计记录。

## 10. 指标

匿名详情页每产生一次前端上报请求，就直接累加一次小时级浏览统计；不做会话去重，也不自动重试。Cloudflare 命中缓存时仍由浏览器调用统计接口。

数据库只保存匿名聚合：

```text
content_type
content_id
hour_bucket
page_views
```

核心指标：

| 维度 | 指标 | MVP 目标 |
| --- | --- | ---: |
| SEO | 已发布页面有效收录率 | >= 70% |
| 搜索 | 零结果率 | < 15% |
| 搜索 | 结果点击率 | >= 45% |
| 数据 | 已发布字段可追溯率 | >= 98% |
| 数据 | 重复实体率 | < 1% |
| 运营 | 有效纠错首次响应 | <= 2 个工作日 |
| 性能 | 缓存命中页面 LCP | < 2.5 秒 |
| 性能 | 移动端 CLS | < 0.1 |

登录用户的 7 日回访、收藏和关注可单独统计。匿名用户无法在不保存标识的前提下精确计算回访率，因此不把匿名回访作为 MVP 硬指标。

指标实现状态：Release A 已提供不含查询词、IP、Cookie 或匿名 ID 的搜索请求与零结果小时聚合。结果点击率仍需先确认“只统计首屏首次点击还是全部点击、分页是否进入分母、客户端缓存返回如何去重”的口径；在该口径确认并完成隐私评审前，`>= 45%` 作为上线后产品观察目标，不得在后台展示为已可计算指标。

## 11. MVP 范围与后续规划

### P0：测试网站

- 作品/人物/厂牌数据模型；
- 后台人工录入人物、作品和厂牌，作品 CSV 导入，图片 manifest 导入，以及审核、版本、发布、隐藏和下架；
- 番号、标题、人物、别名和厂牌搜索；
- 首页、列表、作品详情、人物详情和固定 SEO 页面；
- 图片处理、默认图、S3 清单和来源追踪；
- 注册登录、邮箱验证码、收藏、关注、历史、反馈和关闭账号；
- 匿名小时浏览聚合；
- 备份、监控、审计和限流；
- 广告位占位，不接联盟。

### P1：采集自动化

- 任务、脚本、批次和区域运行合同见 [COLLECTION_PLAN.md](./COLLECTION_PLAN.md)；
- 北京和日本 Python 采集器；
- 来源连接器、增量任务和批次上传；
- 字段归一、重复候选和冲突审核；
- 图片抓取候选和人工确认；
- 可选身体参数和官方社交账号采集。

### P2：流量与商业化

- SEO 专题和内容质量实验；
- 推荐与榜单调优；
- 在流量、政策和页面性能达到门槛后评估广告联盟；
- 数据量和查询指标达到阈值后再评估独立搜索服务。

## 12. 产品决策记录

| 决策 | 结果 |
| --- | --- |
| 产品核心 | 作品浏览与推荐，搜索辅助 |
| 默认“热门”含义 | 最新实际发行，不等于点击量 |
| 文本搜索核心 | 作品番号 |
| 人物/作品混排 | 后台可配置，测试默认作品 : 人物 = 4 : 1 |
| 头像识别 | 不做，不预留公开接口 |
| 图片缺失 | 使用区分人物/作品的默认图 |
| 图片数量 | 详情页每部作品 1 主图 + 最多 3 精选图，共 4 张；列表页仅主图 |
| 匿名行为 | 不保存明细，只按请求聚合浏览量 |
| 注册 | 必须邮箱验证码 |
| 反馈 | 必须登录，人工审核 |
| 关闭账号 | 邮箱验证码 + 状态关闭，不物理删除记录 |
| 广告 | 测试期仅占位，不接联盟 |
| 自动采集 | 后期实施，不能自动发布高风险字段 |
